import {Button} from "react-bootstrap";

const SearchPage = () => {
    return (
        <div>
            <input/>
            <Button variant="primary">Search Movies</Button>
        </div>
    );
};

export default SearchPage;